# Database Configuration

# Connection string for the managed agent database (PostgreSQL)
DB_CONNECTION_STRING=...

# Databricks Configuration

# Host: your-workspace.cloud.databricks.com (without https://)
DATABRICKS_HOST=...
# HTTP path from your SQL warehouse settings
DATABRICKS_HTTP_PATH=...
# Personal access token or OAuth token
DATABRICKS_ACCESS_TOKEN=...
# Catalog name (e.g., 'main' or your custom catalog)
DATABRICKS_CATALOG=...
# Schema name (e.g., 'default' or your custom schema)
DATABRICKS_SCHEMA=...

# API Keys

# Vercel AI Gateway API key
AI_GATEWAY_API_KEY=...